extends layout

block content

  h1 #{title}: #{message.text}

    p Do you really want to delete this Message?

    form(method='POST')
      div.form-group
        input#messageid.form-control(type='hidden', name='messageid', value=message._id )

      button.btn.btn-primary(type='submit') Delete